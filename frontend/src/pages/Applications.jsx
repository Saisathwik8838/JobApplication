import { useEffect, useState } from 'react'; import { get, post } from '../api/client.js'; import { Status } from '../components/Status.jsx';
export function Applications() { const [items, setItems] = useState([]); const [error, setError] = useState(''); const reload = () => get('/api/applications').then(setItems).catch((e) => setError(e.message)); useEffect(reload, []); return <section><h2>Applications</h2>{error && <p role="alert">{error}</p>}{items.map((item) => <article className="application" key={item.id}><h3>{item.job.company} — {item.job.title}</h3><Status value={item.status}/><p>{item.error}</p><h4>Answers</h4>{item.answers.map((answer) => <p key={answer.id}><strong>{answer.question}</strong><br/>{answer.answer ?? 'Needs your input'} <Status value={answer.status}/></p>)}<button onClick={() => post(`/api/applications/${item.id}/retry`).then(reload).catch((e) => setError(e.message))}>Retry safely</button></article>)}</section>; }
