import { useEffect, useState } from 'react'; import { get } from '../api/client.js';
const labels = { jobsDiscovered: 'Jobs discovered', matching: 'Matching', highQualityMatches: 'High-quality matches', prepared: 'Prepared', awaitingApproval: 'Awaiting approval', submitted: 'Submitted', failed: 'Failed', interviews: 'Interviews' };
export function Dashboard() { const [data, setData] = useState(null); const [error, setError] = useState(''); useEffect(() => { get('/api/dashboard').then(setData).catch((e) => setError(e.message)); }, []); if (error) return <p role="alert">{error}</p>; if (!data) return <p>Loading dashboard…</p>; return <section><h2>Today’s pipeline</h2><div className="cards">{Object.entries(labels).map(([key, label]) => <article className="card" key={key}><span>{label}</span><strong>{data[key]}</strong></article>)}</div></section>; }
