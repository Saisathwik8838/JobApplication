import { describe, expect, it } from 'vitest'; import { evaluateEligibility } from '../../src/modules/eligibility/eligibilityEngine.js';
const profile = { candidate: { name: 'Test User', email: 'test@example.com', phone: '1', location: 'Bengaluru' }, education: { degree: 'Bachelor of Technology', branch: 'Computer Science', college: 'Test', graduation_year: 2026 }, experience: { years: 0, level: 'Entry' }, skills: { programming: ['JavaScript'], backend: [], cloud: [], devops: [], databases: [], ai: [], frontend: [], core_cs: [] }, projects: [], preferences: { roles: [], locations: [], remote: true, hybrid: true, onsite: false, employment_types: [] }, salary: {}, rules: { minimum_match_score: 75, auto_prepare: true, require_approval: true } };
describe('eligibility engine', () => { it('rejects a five-year role for a zero-year profile without an LLM', () => { const result = evaluateEligibility(profile, { description: 'At least 5 years of experience is required.', location: 'Remote' }); expect(result.eligible).toBe(false); expect(result.hardFailures.join(' ')).toContain('5+ years'); }); it('allows a compatible entry-level role', () => expect(evaluateEligibility(profile, { description: 'Bachelor’s degree preferred.', location: 'Remote' }).eligible).toBe(true)); });
