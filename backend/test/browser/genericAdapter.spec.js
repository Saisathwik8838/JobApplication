import { test, expect } from '@playwright/test'; import { resolve } from 'node:path'; import { pathToFileURL } from 'node:url'; import { GenericApplicationAdapter } from '../../src/modules/browser/adapters/genericApplicationAdapter.js'; import { ApplicationSession } from '../../src/modules/browser/applicationSession.js';
test('fills supported safe answers on a local fixture', async ({ page }) => { await page.goto(pathToFileURL(resolve(process.cwd(), '..', 'browser', 'adapters', '__fixtures__', 'application.html')).href); const adapter = new GenericApplicationAdapter(); const session = new ApplicationSession({ page, application: {}, job: {}, answers: [{ question: 'What programming languages do you know?', answer: 'JavaScript', status: 'READY' }, { question: 'Years of experience', answer: '0', status: 'READY' }] }); const result = await adapter.fill(session); expect(result.status).toBe('READY_FOR_REVIEW'); await expect(page.locator('#skills')).toHaveValue('JavaScript'); });
test('pauses on CAPTCHA', async ({ page }) => { await page.goto(pathToFileURL(resolve(process.cwd(), '..', 'browser', 'adapters', '__fixtures__', 'captcha.html')).href); const result = await new GenericApplicationAdapter().fill(new ApplicationSession({ page, application: {}, job: {}, answers: [] })); expect(result.status).toBe('MANUAL_INTERVENTION'); });
