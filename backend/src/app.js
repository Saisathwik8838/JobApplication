import express from 'express'; import cors from 'cors'; import pinoHttp from 'pino-http';
import { errorHandler } from './middleware/errors.js'; import { notFound } from './middleware/notFound.js';
import { jobsRouter } from './routes/jobs.js'; import { applicationsRouter } from './routes/applications.js'; import { dashboardRouter } from './routes/dashboard.js'; import { profileRouter } from './routes/profile.js'; import { discoveryRouter } from './routes/discovery.js'; import { automationRunsRouter } from './routes/automationRuns.js';
/** @typedef {{prisma:any, queues:any, profile:import('@job-agent/shared-schemas').CandidateProfile, profileVersion:string, profilePath:string, masterResumePath:string, readMasterResume:()=>Promise<string>, provider:import('./modules/ai/types.js').LLMProvider, logger:import('pino').Logger, config:any, sources:any[]}} AppDependencies */
/** @param {AppDependencies} dependencies */
export function createApp(dependencies) { const app = express(); app.use(cors({ origin: dependencies.config.ALLOWED_ORIGINS.split(','), credentials: false })); app.use(express.json({ limit: '1mb' })); app.use(pinoHttp({ logger: dependencies.logger })); app.get('/health', (_request, response) => response.json({ status: 'ok' })); app.use('/api/jobs', jobsRouter(dependencies)); app.use('/api/applications', applicationsRouter(dependencies)); app.use('/api/dashboard', dashboardRouter(dependencies)); app.use('/api/profile', profileRouter(dependencies)); app.use('/api/discovery', discoveryRouter(dependencies)); app.use('/api/automation/runs', automationRunsRouter(dependencies)); app.use(notFound); app.use(errorHandler); return app; }
