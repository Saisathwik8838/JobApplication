import { Router } from 'express';
/** @param {import('../app.js').AppDependencies} dependencies */
export function dashboardRouter({ prisma }) { const router = Router(); router.get('/', async (_request, response, next) => { try { const [jobs, applications] = await Promise.all([prisma.job.groupBy({ by: ['status'], _count: true }), prisma.application.groupBy({ by: ['status'], _count: true })]); const get = (items, status) => items.find((item) => item.status === status)?._count ?? 0; response.json({ jobsDiscovered: jobs.reduce((total, item) => total + item._count, 0), matching: get(jobs, 'MATCHED'), highQualityMatches: await prisma.jobMatch.count({ where: { result: { path: ['matchScore'], gte: 75 } } }), prepared: get(applications, 'APPLICATION_PREPARED'), awaitingApproval: get(applications, 'AWAITING_APPROVAL'), submitted: get(applications, 'SUBMITTED'), failed: get(applications, 'FAILED'), interviews: get(applications, 'INTERVIEW') }); } catch (error) { next(error); } }); return router; }
